services:
  api:
    build:
      context: ..
      dockerfile: deployment/docker/Dockerfile
    ports:
      - "${API_PORT:-8000}:8000"
    environment:
      RESILIENCELAB_STORE: /data/results
      RESILIENCELAB_SERVER_MODE: "true"
      RESILIENCELAB_DATABASE_URL: postgresql://resiliencelab:resiliencelab@postgres:5432/resiliencelab
      RESILIENCELAB_REDIS_URL: redis://redis:6379/0
    volumes:
      - resiliencelab-data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  worker:
    build:
      context: ..
      dockerfile: deployment/docker/Dockerfile
    command: ["python", "-m", "resiliencelab.controlplane.worker_cli"]
    environment:
      RESILIENCELAB_STORE: /data/results
      RESILIENCELAB_SERVER_MODE: "true"
      RESILIENCELAB_DATABASE_URL: postgresql://resiliencelab:resiliencelab@postgres:5432/resiliencelab
      RESILIENCELAB_REDIS_URL: redis://redis:6379/0
    volumes:
      - resiliencelab-data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-resiliencelab}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-resiliencelab}
      POSTGRES_DB: ${POSTGRES_DB:-resiliencelab}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-resiliencelab}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    volumes:
      - redisdata:/data
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  resiliencelab-data:
  pgdata:
  redisdata:
