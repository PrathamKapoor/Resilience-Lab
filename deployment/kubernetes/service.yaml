apiVersion: v1
kind: Service
metadata:
  name: resiliencelab-api
  labels:
    app: resiliencelab-api
spec:
  type: ClusterIP
  selector:
    app: resiliencelab-api
  ports:
    - name: http
      port: 80
      targetPort: 8000